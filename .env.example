# Optional: override API origin in production builds (e.g. https://api.example.com)
# Leave empty in development to use Vite's proxy to the FastAPI backend.
VITE_API_BASE_URL=

# Dev-only: FastAPI server used by Vite proxy (not exposed to the browser)
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
